namespace Signals
{
    public class OnElementSignal
    {
        public readonly Element Element;

        public OnElementSignal(Element element)
        {
            Element = element;
        }
    }
}